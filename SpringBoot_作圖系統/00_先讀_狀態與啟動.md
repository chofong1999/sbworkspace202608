# 先讀：狀態與啟動

- 系統：`AI通用作圖系統`
- 系統版本：`0.5`
- 系統狀態：`可使用`
- 工作代號：`WRK-20260814-SBILLUSTRATED`
- 工作名稱：`Spring Boot 課程筆記圖文製作與截圖管理`
- 最後更新：`2026-08-14 09:57:46 +08:00`

> 本文件只負責系統身分、是否可用、工作身分與流程入口。視覺規格看`11`，當前圖片、階段、批次與下一步只看`12`，樣板看`20`；不得把這些易變現況複製回本文件。
> 本系統管理做圖方法與品質，不管理AI員工身分、交接世代或核心交接狀態。若同一工作存在交接系統，本系統在第一次正式寫入前自動依該制度取得工作權；只讀操作不接手。

## 正式部署

```text
C:\sbworkspace202608\
├─ SpringBoot_AI交接資料\
├─ SpringBoot_作圖系統\
├─ SpringBoot_課程筆記\
└─ SpringBoot_圖文作業資料\
```

`SpringBoot_作圖系統`、`SpringBoot_課程筆記`與`SpringBoot_圖文作業資料`同層。正式筆記與原始素材仍在既有`SpringBoot_課程筆記`，不因建立本系統搬移、重驗或重建；候選、衍生圖與內部QA分流到`SpringBoot_圖文作業資料`。詳細位置以`10_本工作做圖說明.md`為準。

## 寫入前自動取得工作權

1. 使用者要求任何會修改圖片、視覺規格、候選、樣板、圖片契約、QA紀錄或作圖系統狀態的功能時，在本次功能第一次寫入前執行交接前置檢查；明確指令與自然語言要求都適用。
2. 只讀查看、分析、規劃或檢查狀態時不取得工作權，也不修改交接資料。
3. 找到同一工作的交接系統時：
   - 本對話已是目前工作者：直接繼續，不重複接手。
   - 狀態允許接手且沒有工作者：依該交接制度自動以最低成本接手；符合零差異返回時，不重讀不必要資料、不寫接手日誌、不增加世代。
   - 其他員工正在工作、工作身分不一致或狀態不允許安全接手：停止正式寫入，只回報真正阻塞原因。
4. 自動接手只執行既有交接制度，不得修改、降低、跳過或繞過其條件。
5. 找不到交接系統時，本作圖系統可獨立運作。
6. 交接系統的手動接手指令仍可使用，但不是正常使用作圖功能前的必要操作。
7. 首次建立的只讀分析階段不接手；使用者確認正式建立後，在第一次寫入前才執行本節。

## 啟動判斷

依序執行第一個符合的流程：

- 同一對話仍可靠記得`01`與`11`，且兩者版本沒有變時，正常續作只重讀`12`及本張引用的最少規則／方法。
- 全新對話或記憶不可靠時，才依下列入口讀取必要文件；正常工作不預讀`30`、`40`、`50`或`90`全文。

1. `系統狀態＝未建立`：使用者要求正式建立時，讀`80_按需/00_首次建立.md`。
2. 只查看或檢查狀態：讀本文件及要求涉及的最少文件，不改狀態，也不取得交接工作權。
3. 建立或比較候選方案、確認樣板：讀`21_候選方案登錄.md`及`80_按需/10_候選方案與樣板確認.md`。
4. 開始單張圖片：全新對話讀`01_通用作圖流程.md`、`11_本工作視覺規格.md`及`12_當前圖片與批次.md`；回來的同一對話依上方增量規則讀取。
5. 開始批次：必須由`12`確認允許批次，並讀`80_按需/40_批次製作.md`。
6. 發生錯誤、規則衝突、重複失敗、成果漂移，或使用者指出成果有問題：先讀`80_按需/20_異常與失敗處理.md`，再用現象／標籤搜尋`23_規則與回歸索引.tsv`的命中列；不得為此預讀整份索引，找不到時才定向搜尋來源文件。
7. 使用者變更長期規格、樣板或驗收方式：讀`80_按需/30_規格更新與影響評估.md`。
8. 使用者提供新版通用作圖系統：讀`80_按需/50_系統更新.md`；不得當成普通視覺規格更新。
9. 工作SOP、品牌指南或其他權威規格新增或更新：讀`13_工作規格與來源.md`及`80_按需/60_工作規格同步.md`。
10. 首次建立、規格定案、樣板確認、批次完成、重要阻塞或準備交接：若有交接系統，讀`80_按需/70_交接系統協作.md`。

## 明確指令

訊息第一個非空白字元為全形`！`或半形`!`，且第一段正好是下列名稱時，視為明確指令。自然語言仍可表達相同意思。

| 指令 | 執行條件 |
|---|---|
| `！首次建立` | 只限未建立 |
| `！開始作圖` | 工作規格已建立，且來源與本張契約可用 |
| `！測試候選` | 已說明候選目的、差異及比較方式 |
| `！確認樣板` | 指定樣板與確認範圍；不得由AI代替使用者主觀確認 |
| `！批次製作` | 已有適用的確認樣板，且允許批次 |
| `！檢查圖片` | 指定成果；預設執行最低充分檢查 |
| `！更新規格` | 指定變更或提供新規則；先做影響評估 |
| `！更新作圖系統` | 已提供同一模組的新版套件；讀系統更新流程 |
| `！同步工作規格` | 已指定工作規格來源，且來源可讀 |
| `！同步交接摘要` | 同一工作存在交接系統；若尚未取得工作權，首次寫入前自動依該制度檢查並接手 |
| `！檢查狀態` | 只讀，不修改成果 |

條件不符時不猜成其他寫入功能；只回報不符條件與一個最短下一步。

## 階段與狀態

系統狀態只使用：`未建立`、`建立中`、`可使用`、`暫停待處理`、`已停止`。

當前階段只使用：

- `未建立`
- `建立工作規格`
- `候選測試`
- `等待使用者確認`
- `單張製作`
- `批次製作`
- `修正異常`
- `系統更新`
- `同步工作規格`
- `同步交接摘要`
- `完成`

成果狀態、規則狀態與QA證據層級以`03_格式與狀態定義.md`為準。

## 每次動手前的必要閘門

在修改圖片前，必須能從`12_當前圖片與批次.md`直接回答：

1. 正在處理哪個來源與哪個成果？
2. 使用哪個視覺規格及確認樣板？
3. 本張適用哪些強制規則？
4. 哪些部分可調整？
5. 使用哪個候選方案或正式方案？
6. 完成前如何驗證，生產證據與驗證證據是否獨立？

缺少任何一項且會影響成果時，不得靠推測開始製作。

使用者不必先要求AI「建立契約」。若作圖意圖與必要輸入已清楚，AI應自行建立最短充分契約並繼續；只有缺少會實質改變成果且沒有安全預設的資訊時才詢問。

## 完成前的必要閘門

不得只說「已檢查」。交付前必須：

1. 依本張契約逐項核對強制規則。
2. 對每項提供最小必要證據、證據來源與獨立性層級；不足就標示`未驗證`。
3. 區分`內部QA通過`與`使用者已確認`。
4. 發現偏離時修正或停止，不得修改規則讓錯誤成果變成合格。

## 資訊衝突權威

1. 使用者在目前對話中的明確指示。
2. `12_當前圖片與批次.md`中經授權的本次例外。
3. `13_工作規格與來源.md`中標為`權威來源`且目前有效的規格原文。
4. `11_本工作視覺規格.md`中的已確認現行規則。
5. `20_樣板與元件登錄.md`中已確認且適用的樣板／元件。
6. `10_本工作做圖說明.md`。
7. `02_通用品質規則.md`。
8. `01_通用作圖流程.md`。
9. 實際來源與可重現的驗證結果。
10. `30_製作與QA紀錄.md`及歷史成果。
11. AI建議或推測。

工作規格只有在`13`明確登錄其權威程度、適用範圍與版本後才取得上述順位。實際存在的成果能證明「現在有什麼」，不能單獨證明「這樣符合要求」。歷史紀錄不能自行改變現行規則。

## 文字與檔案安全

- 文字先以UTF-8讀取；無法可靠解讀時停止，不得直接改寫。
- 原始素材預設唯讀，不得直接覆蓋。
- 不得未經授權刪除、覆蓋或移動使用者既有成果。
- 不得把其他專案、示例或AI建議自動當成本工作的規格。
- `40_工作規格`與`50_工作方法`可保存本工作專用資料，但不得反向改寫本文件、`01`至`03`或其他通用核心。

