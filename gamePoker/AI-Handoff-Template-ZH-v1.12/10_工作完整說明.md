# 工作完整說明

- 最後更新：`2026-08-26 15:15:08 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。
> 本文件給全新 AI 閱讀，只放仍有效的長期介紹；版本、批次、目前階段與下一步不得放在這裡。

## 目標與完成條件

將舊桌面版「田忌撲克」重製為可由簡易遊戲平台以 iframe 載入的 Web 遊戲。完成條件為：保留已確認的舊版玩法；Java／Spring Boot 後端掌握牌局、洗牌、發牌、選牌合法性、牌型、回合與勝負；前端可完成電腦與雙人對戰；雙人房間可配座、斷線釋位並保留牌局；API、WebSocket 與 iframe 整合可實際運作；使用者要求的整合資料可由平台傳入。

## 範圍

- 包含：田忌撲克遊戲核心、Spring Boot API、WebSocket 在線／離線處理、測試用房間座位、電腦對戰、雙人逐輪對戰、HTML／CSS／JavaScript、iframe 遊戲入口、遊戲結果。
- 不包含：平台登入、商城、聊天室、遊戲選擇頁、正式共用對戰房間系統、平台積分與商城結算。

## 成果與位置

- 主要成果位置：`C:\sbworkspace202608\gamePoker`
- 重要入口與依賴：入口 `src/main/resources/static/poker_client.html`；前端 `poker.js`、`poker.css`；後端套件 `src/main/java/com/example/demo/poker/`；Java 17；Spring Boot 4.1.1；Spring Web MVC；Spring WebSocket。
- 現成版本識別取得方式：`pom.xml` 的 `0.0.1-SNAPSHOT`，搭配本交接資料的世代與最新日誌辨識。

## 目前有效的決定與限制

- 遊戲固定入口為 `http://<遊戲主機>:8080/poker_client.html`；目前區域網路測試主機為 `10.10.1.213`，前端 API 使用相對路徑，不寫死 IP。
- 正式房間系統由平台獨立負責；目前遊戲內只保留測試所需的座位與對局連線管理。
- 雙人正式流程以舊專案 `referenceui.ReferenceGameUI` 為依據：每輪分別選 3／5／5 張，雙方確認後才公開該輪結果，再進下一輪。
- 測試版資料保存在伺服器記憶體；Spring Boot 重啟後房間與進行中對局消失。
- 玩家離開只釋放座位，不結束或重置對局；補位者接手該座位既有進度。

## 重要背景、名詞與風險

- 舊遊戲來源：`C:\work\Homework02`。重要類別為 `model.Card`、`Game`、`Player`、`Round`、`util.Sort`、`GameTool`、`controller.Game_UI` 及 `referenceui`。
- 課程參考：`C:\sbworkspace202608\sbemployeetest`、`C:\sbworkspace202608\websocketChat`、`C:\jscode\day5`、`C:\jscode\day6`。
- 玩家一直接開頁面、玩家二由另一台電腦的外框 iframe 載入是主要整合測試情境；跨來源 iframe 不可直接讀取外框 DOM。
- 待整合的玩家姓名可先由 iframe query parameter 傳入，正式版可再討論 `postMessage`；目前尚未實作姓名參數。
