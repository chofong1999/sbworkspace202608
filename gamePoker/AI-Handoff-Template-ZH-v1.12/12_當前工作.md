# 當前工作

- 最後更新：`2026-08-26 16:22:58 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。
> 這是「現在要做什麼」與當前成果狀態的唯一正式來源。正式交接時更新；工作中只有復原價值高時才更新最小檢查點。
> 不要重複完整背景、長期規則、歷史或外部子系統細節。

## 目前目標與進度

正在把 Web 測試版校正回 `Homework02` 的原始結構與寫法。本輪已修改核心類別、分層、自動選牌與 UI 操作，但使用者指出 `Round` 等類別仍被改寫成不同的命名、Java 語法及壓縮排版，未達到「盡量遵照原有程式」的要求；目前暫停並交接，不得把本輪修改視為完成。

## 本輪臨時要求

- DAO／資料庫本階段暫不連接。
- 原專案已有的資料夾、類別、方法、命名、運算流程與易讀排版要盡量直接保留，不得只保留功能後用另一種寫法重製。
- 舊結構不足或因 Web／連線必須新增資料夾、類別或修改設計時，先與使用者討論，不自行決定。

## 未完成、阻塞與待決定

- 未完成：`Round`、`Card`、`Game`、`Player`、`CardSort`、`GameTool` 等仍需逐檔改回接近舊原始碼的類名、方法名、控制結構與排版；目前 `CardSort` 應重新評估是否恢復舊名 `Sort`。Web UI 尚未瀏覽器驗證；玩家姓名傳入尚未實作。
- 阻塞：無。
- 待使用者決定：舊版沒有的 `GameRoom`、`PlayerState`、`HandResult`、`WebSocketConfig`、`PokerWebSocketHandler` 應如何建立額外 package；助手提出過 `model／config／websocket`，但尚未獲確認。正式平台姓名整合方式也尚待決定。

## 下一步

- 位置：`C:\work\Homework02\src\main\java` 與 `src/main/java/com/example/demo/`。
- 要完成什麼：逐檔對照舊原始碼，優先直接搬移其 package、類別、方法、命名、迴圈／switch 寫法與註解；只在 Web 介接邊界做必要修改。先修正 `Round` 圖示所指出的不同寫法，再核對其餘核心檔案。
- 不得改變什麼：逐輪 3／5／5 流程、點牌選擇／取消操作、離開保留對局、既有 roomId 與 mode 參數相容性。
- 如何確認完成：直接頁面及 iframe 各以兩個不同姓名加入同一房間；雙方畫面顯示正確姓名；玩家二可確認；離開補位後姓名更新且牌局保留；`mvnw.cmd test` 通過。

## 當前成果狀態

狀態只使用：`已完成`、`已產出未驗證`、`部分完成`、`驗證未通過`、`受阻`、`無法可靠完成`。

- 狀態：`部分完成`
- 現成版本識別：`0.0.1-SNAPSHOT；交接世代 1／LOG-0001`
- 本工作階段變動成果：新增／重整 `model`、`util`、`controller`、`service.impl`、`exception`；牌型判斷改由 `Round`、自動選牌改由 `GameTool`，新增排序與自動選牌 UI，選滿時替換最早選牌。但核心程式被重新改寫，仍不符合忠實沿用舊寫法的驗收要求。
- 適用的強制RULE-ID：`RULE-0001`、`RULE-0002`、`RULE-0003`、`RULE-0004`、`RULE-0006`、`RULE-0008`
- 驗證證據或未通過項：最後一次 `mvnw.cmd clean test` 為 4 tests、0 failures；僅證明目前可編譯及既有測試通過。本輪 UI 未做瀏覽器流程驗證。使用者已明確判定核心寫法與舊版不同，因此 RULE-0008 驗收未通過。
- 明示降級或限制：測試版房間只存在記憶體；伺服器重啟會清除；目前沒有平台登入身分驗證。

使用`已完成`前，所有適用強制規則須有通過證據、明確不適用，或使用者明示授權的降級結果；有契約者還須符合其`pass_evidence`。

## 工作中檢查點

正式交接後填`無`。目前工作者只有在重要里程碑、復原成本明顯升高或高風險修改前才更新：

- 檢查點時間：`無`
- 已完成：`無`
- 未驗證：`無`
- 下一步：`無`
- 必要成果入口：`無`

工作中檢查點不得清空工作者、改開始時間、追加日誌、增加世代、更新最近成功交接時間或假稱已形成可正常接手的正式快照。

## 外部子系統摘要

只有實際使用子系統時填寫；沒有則寫`無`。每個子系統最多保存：

- 名稱／位置／版本：
- 階段／阻塞：
- 下一步：
- 詳細入口：
- 最後同步：
