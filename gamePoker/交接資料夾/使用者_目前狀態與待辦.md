# 使用者目前狀態與待辦

- 專案：`簡易遊戲平台－遊戲管理、田忌撲克與房間整合`
- 工作：`Game Management、Poker 與 Room／Chat 串接`
- 最後更新：`2026-09-03 18:11:26 +08:00`
- 對應世代／最新日誌：`11／LOG-0011`

> 本文件是使用者摘要；正式狀態以 `00_先讀_身分與啟動.md` 與 `12_當前工作.md` 為準。

## 目前摘要

- 整體狀況：`部分完成`
- 正式成果：`C:\git\gammPlatform`，`main c8b3b76`，與 `origin/main` 一致且工作區乾淨。
- 最近交接快照：Poker 中途離開及正常結束已接上 Room lifecycle；外層 Chat 會在遊戲開始後接手房間 WebSocket，並在收到 `ROOM_FINISHED` 後停用房間訊息輸入。JavaScript syntax 與後端 compile 通過，但尚未完成雙瀏覽器實機驗證。
- 是否可交給下一位 AI：`是；狀態為可接續。`

## 已完成

- 正式 Poker 與測試 Tjpoker 不再提供 restart 流程。
- 真人自動選牌只處理目前輪次；電腦 AI 仍使用完整三輪配置。
- 三輪倒數由伺服器統一控制，FINISHED 後隱藏選牌操作。
- Poker WebSocket 關閉不再直接清除玩家 token；主動離開改由 leave API 處理。
- Poker 已在正常結束與中途離開時通知 Room lifecycle。
- 遊戲中的房間聊天改由外層 Chat 頁面持有 WebSocket。
- 15 頁專題簡報、畫面素材及渲染結果已存在於 `PPT/`。
- 跨組問題文件集中於 `GamePlatformInfo/`。

## 目前待辦

1. 用兩個瀏覽器驗證等待區聊天、遊戲中聊天、正常結束、中途離開與意外斷線。
2. 修正房間 WebSocket 未 OPEN 時仍清空未送出訊息的行為。
3. 與 Chat／Lobby 組確認兩個 Room finished Listener 是否合併、房間資料是否保留，以及是否需要事件重送。
4. 完成 PPT 的最後重新匯出、ER 圖核對與版面驗證；決定是否將被忽略的 `PPT/_work` 製作來源納入 Git。
5. 其他組員範圍未再次明確授權前只讀檢查，不直接修改。

## 仍需留意的跨組問題

- 先前完整 Maven 測試曾出現 Board／User 格式規則及既有 DB 重複初始化 admin 的問題；本輪只執行 compile，未重新宣稱完整測試通過。
- Poker／Tjpoker 的 Service 使用同步方法，所有房間共用同一把鎖；大量同時遊戲時可能互相等待。
- GameMode 仍需確認 `(gameId, modeCode)` 資料庫唯一約束與既有資料更新策略。
- Board 與正常 Lobby 傳給 Poker 的使用者 ID 契約需保持一致。
- Quiz 的題目正解暴露、交卷完整性及富文字安全問題仍由 Quiz 組處理。
- 前端 API base 與 Vite 多頁正式建置仍需要團隊統一。
