# 當前工作

- 最後更新：`2026-09-01 16:17:00 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。
> 這是「現在要做什麼」與當前成果狀態的唯一正式來源。

## 目前目標與進度

主要成果位於團隊專案 `C:\git\gammPlatform`。舊 `tjpoker` 測試版已保留，正式 `poker` 與前端分層已建立。原 `modules/game/system` 已改名為 `modules/game/management`（遊戲管理系統），Model、DTO、Repository、ServiceImpl、公開／Admin Controller、預設資料與房間串接說明均已建立。目前優先修正 SQLite 建立 `game_modes.mode_id` 時漏掉 `integer`、導致資料庫值為 NULL 的問題；房間傳值串接暫停。

## 本輪臨時要求

- 團隊目前只使用 main；提交前確認沒有改到其他組員檔案。
- 遊戲管理系統放在 `backend/src/main/java/com/example/demo/modules/game/management`，依 `config／model／dto／repository／service／service.impl／controller` 分層。
- 後端只能修改 `game/management`、`game/poker`、`game/tjpoker`；其他模組需先與使用者討論。前端 Poker 可修改。
- 目前先完成遊戲管理系統，再做房間系統；房間傳給遊戲的 userId／seat／roomId 等介接延後。

## 未完成、阻塞與待決定

- 未完成：修正並實測 `game_modes.mode_id` 自動主鍵。Hibernate 7.4.5＋SQLite 目前輸出 `mode_id,`，缺少 `integer`；初始化後四筆 mode_id 均為 NULL，而 `games.game_id` 正常。
- 未驗證：剛在 `GameMode.modeId` 加入 `@Column(name = "mode_id")` 作最小實驗，尚未 clean compile／啟動驗證；若無效需撤回，不得宣稱已修好。
- 未完成：遊戲管理公開／Admin API 的完整實際回傳驗證，以及交付房間組員的最終文件核對。
- 延後：正式房間系統與 Game／GameMode／Poker 傳值串接。
- 注意：SourceTree 曾因已刪除的臨時檔 `rename_game_management.js` 留有舊畫面而 staging 報錯；檔案實體與 Git 索引均已確認不存在。暫存區包含本輪大量 management／poker／tjpoker／前端變更，提交前須再次限定範圍核對。
- 阻塞：無。
- 待決定：Admin 權限驗證正式介接方式；房間與獨立遊戲的 ticket 格式及 Match 資料欄位。

## 下一步

1. 對照正常 `Game` 與異常 `GameMode`，clean compile 後檢查 Hibernate DDL；Model 主鍵應維持相同的 `@Id + IDENTITY + Long` 基本寫法。
2. 修到 DDL 明確為 SQLite 可自動編號的整數主鍵，重新建立資料庫。
3. 透過遊戲管理 API 確認四筆 GameMode 的 modeId 為 1、2、3、4，而非只看啟動成功。
4. 檢查公開／Admin API、文件、舊 `system` 殘留引用與允許修改範圍。
5. 通過 clean compile／啟動／API 後，再交付房間系統夥伴使用；暫不繼續房間傳值。

## 當前成果狀態

- 狀態：`部分完成`
- 現成版本識別：`gameplatform 0.0.1-SNAPSHOT；main；交接世代 4／LOG-0004`
- 本工作階段變動成果：Game System 已更名並移至 `game/management`；完成 Game／GameMode 管理分層、ServiceImpl、Controller、預設正式 POKER 與停用 TJPOKER 資料、房間串接說明；建立正式 `poker` 並保留 `tjpoker`；前端圖片與三個頁面檔依 Games/poker、Games/tjpoker 分層；移除錯置的 LobbyUser 類別；產生 Poker 遊戲圖示。
- 驗證證據：管理模組改名後 `mvnw.cmd clean compile` 曾成功；Spring Boot 能啟動且 Game／GameMode 初始化會執行；實際 DDL 與資料庫檢查揭露 mode_id 缺型別及 NULL，故整體仍是驗證未通過。
- 未驗證或限制：`mode_id` 尚未修復；公開／Admin API 未做最終完整驗證；目前 application.properties 使用 create-drop；Poker 前端定時完整重繪問題暫不處理。
- 適用的強制RULE-ID：`RULE-0001`、`RULE-0002`、`RULE-0003`、`RULE-0004`、`RULE-0006`、`RULE-0008`、`RULE-0009`、`RULE-0010`、`RULE-0011`、`RULE-0012`

## 工作中檢查點

已確認 `Game` 與 `GameMode` 的主鍵原始宣告相同；問題發生於 Hibernate 建表階段，早於 `GameDataInitializer`。剛加入 `@Column(name = "mode_id")` 尚未驗證，接續時先 clean compile／啟動看 DDL，無效即撤回。

## 外部子系統摘要

- 名稱／位置／版本：團隊 Git 專案；`C:\git\gammPlatform`；main
- 階段／阻塞：遊戲管理系統主要分層已產出，但 GameMode 自動主鍵驗證失敗。
- 下一步：只續作 `modules/game/management` 的 mode_id 修正與 API 驗證。
- 詳細入口：GitHub `GamePlatform_Project` 的 main。
- 最後同步：`2026-09-01 16:17:00 +08:00`
