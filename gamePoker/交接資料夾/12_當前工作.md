# 當前工作

- 最後更新：`2026-09-02 10:38:00 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。
> 這是「現在要做什麼」與當前成果狀態的唯一正式來源。

## 目前目標與進度

主要成果位於團隊專案 `C:\git\gammPlatform`。正式 `poker`、舊 `tjpoker` 測試版與遊戲管理系統均已建立。Poker 最近加入伺服器統一控制的三秒結果停留與避免無變化時重繪。GameMode 主鍵問題已在 main 修正並曾確認 SQLite 產生非 NULL ID。現在等待其他隊員刪除會與 User 模組衝突的整個 `shared` 後，再做完整啟動驗證；房間傳值串接仍暫停。

## 本輪臨時要求

- 團隊目前只使用 main；提交前確認沒有改到其他組員檔案。
- 遊戲管理系統放在 `backend/src/main/java/com/example/demo/modules/game/management`，依 `config／model／dto／repository／service／service.impl／controller` 分層。
- 後端只能修改 `game/management`、`game/poker`、`game/tjpoker`；其他模組需先與使用者討論。前端 Poker 可修改。
- 目前先完成遊戲管理系統，再做房間系統；房間傳給遊戲的 userId／seat／roomId 等介接延後。
- 本輪只做檢查與討論；未修改團隊專案程式。

## 未完成、阻塞與待決定

- 等待外部提交：其他隊員預計刪除 `backend/.../shared`；目前 shared.Role／shared.User 與 User 模組同名實體衝突，造成 Spring contextLoads 失敗。未發現其他模組直接 import shared，理論上可安全移除，但刪除後仍須重跑完整測試。
- 未完成：遊戲管理公開／Admin API 的完整實際回傳驗證，以及交付房間組員的最終文件核對。
- 延後：正式房間系統與 Game／GameMode／Poker 傳值串接。
- 專案一致性：`backend/bin` 仍被 Git 追蹤，含舊 class、另一份不同內容的 pom.xml 及舊 shared class。main e71bdae 已加入忽略規則，但既有追蹤檔仍須由團隊取消追蹤；唯一正式 POM 應為 `backend/pom.xml`。
- 整合風險：SecurityConfig 除登入外全部要求 JWT，但 Poker HTTP／WebSocket 尚未統一 JWT 傳遞；正式身分不應信任網址 userId。User、Room、Poker 組需先約定 JWT subject、儲存位置、HTTP Header 與 WebSocket 驗證方式。
- 部署風險：Game 入口使用 `/src/...`，Vite 尚未設定多頁面輸出；`create-drop`、寫死 JWT secret、各模組 CORS 不一致也只適合目前測試。
- 跨模組資料：Board 有獨立 Member，User 模組另有 User；跨模組 User ID 唯一來源尚待團隊統一。
- 阻塞：無；目前只是等待隊員提交 shared 刪除。
- 待決定：Admin 權限、JWT／WebSocket 契約、房間與遊戲 ticket／Match 資料；雙人再來一局建議採邀請／接受流程，電腦模式可直接重開。

## 下一步

1. 確認隊員刪除 shared 的提交已進入 main，並先確認工作區乾淨。
2. 執行 `mvnw.cmd clean test`；再啟動後端驗證 Game／GameMode 預設資料與非 NULL modeId。
3. 由團隊處理已追蹤的 `backend/bin`；只加 `.gitignore` 不會自動取消追蹤。
4. 驗證遊戲管理公開／Admin API 與房間串接文件；暫不實作房間傳值。
5. 與 User／Room 組先定 JWT 與 WebSocket 身分契約，再調整 Poker；正式 User ID 由後端驗證身分取得。
6. 部署前處理 Vite 獨立 HTML 輸出、CORS、JWT_SECRET 環境變數及 `ddl-auto`。

## 當前成果狀態

- 狀態：`部分完成`
- 現成版本識別：`gameplatform 0.0.1-SNAPSHOT；main e71bdae；交接世代 5／LOG-0005`
- 本工作階段變動成果：團隊專案程式無修改；完成 Poker 最近更新與全專案一致性只讀稽核，整理 JWT／Room／Poker 後續契約建議。
- 驗證證據：143 個後端原始檔成功編譯；Poker Controller 1、Poker Service 8、Game Management 1 項測試通過；11 項中只有 contextLoads 因 shared.Role 與 user.Role 衝突失敗。兩份 Poker JS 語法檢查通過。GameMode ID 曾確認為非 NULL。
- 未驗證或限制：shared 尚未刪除，完整 Spring 啟動仍失敗；前端未安裝 node_modules，尚未完成 Vite build；公開／Admin API 與部署路徑未做最終驗證。
- 適用的強制RULE-ID：`RULE-0001`、`RULE-0002`、`RULE-0003`、`RULE-0004`、`RULE-0006`、`RULE-0008`、`RULE-0009`、`RULE-0010`、`RULE-0011`、`RULE-0012`

## 工作中檢查點

已檢查 main 172eb53 的 Lobby 房間系統，確認 rooms 缺 modeId、room_players 只有帳號且未保證順序，另有前後端 hostAccount／playerAccount 欄位不一致。依使用者授權先只修改 game/poker：PokerGameService 新增可由房間系統指定 1／2 號座位的 join 多載，舊 join 保留相容；加入座位占用、重連座位不一致與電腦模式座位驗證。`mvnw.cmd test` 通過 15 tests、0 failures。下一步等待房間隊員完成 modeId、userId、seatNo、STARTED 狀態與查詢 Service 後，再接 Poker Controller；目前三個 Poker 檔案尚未提交。

## 外部子系統摘要

- 名稱／位置／版本：團隊 Git 專案；`C:\git\gammPlatform`；main e71bdae
- 階段／阻塞：遊戲管理與 Poker 單元測試可通過；完整啟動暫受 shared 重複實體阻擋。
- 下一步：shared 刪除後跑完整測試，再驗證 Management API；房間傳值仍延後。
- 詳細入口：GitHub `GamePlatform_Project` 的 main。
- 最後同步：`2026-09-02 10:38:00 +08:00`
