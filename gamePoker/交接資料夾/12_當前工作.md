# 當前工作

- 最後更新：`2026-09-07 16:20:06 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。

## 目前目標與進度

正式成果位於學校電腦 `DESKTOP-A4QEQPK` 的 `C:\git\gammPlatform`。RPG Web 移植仍在依 Homework03 UI 程式碼與原版截圖核對忠實度，成果為`部分完成`。本輪另完成正式發布前的 API 權限收斂與同源 API 修正。

## 本輪已完成

- 安全設定已移除「所有 API 放行」與跨來源萬用字元；登入／註冊／靜態檔及公開遊戲型錄維持公開，其他平台 API 改為必須帶 JWT，管理 API 限 ADMIN。
- 移除 Chat 的測試登入／Session API 與重複的 Board CORS 設定；WebSocket 設定移除跨來源萬用字元。
- 新增正式設定檔 `application-prod.properties`：啟用 `prod` 時 JWT 必須從 `JWT_SECRET` 環境變數取得，並停用 SQL 輸出與 open-in-view。
- 修正 Render 遊戲管理頁仍強制連到 HTTP `:8080`，改採同源 API。
- 修正 Lobby 與等待房間頁 API 沒有附加 JWT，避免權限收斂後誤顯示「後端未啟動」。
- 修正 RPG 技能配置：第一次點擊不再重建技能清單，因此雙擊可正確攜帶技能；右鍵卸下行為保留。
- 已撤回圖靈機前端的嘗試性 JWT 修改；圖靈機目前完全交由其負責隊員處理，未留下工作區差異。

## 驗證結果

- 安全設定整合測試 4 項通過：公開靜態頁、未登入 API 401、玩家不可存取 ADMIN、已刪除測試 API 回 404。
- 完整 Maven 測試 70 項通過，0 failure、0 error、0 skipped。
- RPG JavaScript syntax 與本輪 `git diff --check` 通過。
- 正式成果已推送：`main 9237123 harden-production-security-and-fix-admin-api-origin`、`main 9979d87 fix API route`；本機與 `origin/main` 同步且工作區乾淨。

## 尚未完成與已知差異

- RPG 尚未完成戰鬥角色／敵方資訊、勝敗結算視窗與裝備特性完整一致性；使用者要求分階段處理，下一階段再處理。
- RPG 角色數量仍維持 Web 版最多 3 名，Homework03 原版沒有此限制；需使用者決定後才能修改。
- Chat／Room WebSocket 尚未完整驗證 JWT，Board 部分 API 仍信任前端傳入的會員 ID；這是正式發布前仍需獨立處理的高優先安全項目。
- 圖靈機在權限收緊後 API 請求未帶 JWT，造成「載入遊戲失敗」；已定位但依使用者要求不修改，交由圖靈機隊員自行處理。

## 下一步

1. 等圖靈機隊員自行修正 JWT 請求後再協助驗證，不要修改其前端檔案。
2. 回到 RPG：先設計並實作明確的戰鬥勝敗結算畫面，再處理角色／敵方資訊與狀態 UI。
3. 若要正式部署，Render 必須設定 `SPRING_PROFILES_ACTIVE=prod` 與高強度 `JWT_SECRET`；若前後端不同網域，另設定 `CORS_ALLOWED_ORIGIN_PATTERNS`。
4. 後續以模組負責人處理 Chat／Room WebSocket JWT 與 Board 水平授權，避免跨組直接修改。

## 當前成果狀態

- 狀態：`部分完成`
- 正式成果：`C:\git\gammPlatform`
- 本機版本：`main 9979d87`，工作區乾淨。
- 遠端版本：`origin/main 9979d87`，已同步。
- 參考截圖：`C:\git\sbworkspace202608\gamePoker\交接資料夾\RPG_Homework03原版畫面參考`
