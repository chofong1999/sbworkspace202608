# 當前工作

- 最後更新：`2026-08-28 16:13:25 +08:00`

> 工作身分以`00_先讀_身分與啟動.md`為準。
> 這是「現在要做什麼」與當前成果狀態的唯一正式來源。

## 目前目標與進度

主要成果已由獨立測試專案移入團隊共用專案 `C:\git\gammPlatform`。Poker 測試版可供功能測試，接下來依序完成遊戲資料系統、把 Poker 拆成獨立遊戲、再建立正式房間系統。目前 Game System 只完成 Model、DTO、Repository 與 Service interface，尚未完成實作與 Controller。

## 本輪臨時要求

- 團隊目前只使用 main，不再使用額外功能分支；每次工作前先 Pull，提交前確認沒有改到其他組員檔案。
- Game System 放在 `backend/src/main/java/com/example/demo/modules/game/system`，依 `model／dto／repository／service／service.impl／controller` 分層。
- 大廳未正式介接前，userId 查詢提供明確測試資料開關；正式介接後再切換為查詢 shared User。

## 未完成、阻塞與待決定

- 未完成：GameSystemServiceImpl、LobbyUserServiceImpl、Controller、預設 Poker／GameMode 初始化、錯誤處理與 API 測試。
- 未完成：將測試版 Poker 進一步拆除房間責任，改為接收正式房間／對局資料。
- 未完成：正式房間系統與 Game／GameMode／Poker 串接。
- 阻塞：無；開始前先 Pull 最新 origin/main。
- 待決定：Admin 權限驗證正式介接方式；房間與獨立遊戲的 ticket 格式及 Match 資料欄位。

## 下一步

1. 在 `C:\git\gammPlatform` Pull 最新 main，確認工作目錄乾淨。
2. 完成 `modules/game/system/service/impl` 與 Controller。
3. 建立預設 Game：田忌撲克，以及 COMPUTER／PLAYER 兩筆 GameMode。
4. 完成前台查詢及 Admin 管理 API；權限尚未接入時明確標示限制。
5. 完成 LobbyUser 測試／正式資料切換。
6. 執行 clean compile、Spring Boot 啟動與 API 測試；確認沒有修改其他模組。

## 當前成果狀態

- 狀態：`部分完成`
- 現成版本識別：`gameplatform 0.0.1-SNAPSHOT；main；交接世代 3／LOG-0003`
- 本工作階段變動成果：Poker 已整合到 `modules/game/tjpoker`；前端 Poker 已移入團隊 frontend；建立 Game System 的 Game、GameMode、DTO、Repository、Service interface；Poker WebSocket 統一移到 `tjpoker/server` 並改名為 TjpokerWebSocketConfig／TjpokerWebSocketHandler；團隊分支已合併回 main。
- 驗證證據：Poker 整合後 `mvnw.cmd clean test` 曾通過 1 test；Poker join API 曾回傳 HTTP 200；WebSocket 改名後 `mvnw.cmd clean compile` 成功，Spring Boot 已在測試連接埠成功啟動，同名 Bean 衝突消失。
- 未驗證或限制：Game System 尚無 ServiceImpl／Controller，不能視為可使用；正式房間與獨立遊戲尚未製作；目前 application.properties 使用 create-drop；Poker 前端仍有定時完整重繪造成 hover 反覆重置，測試版暫不修。
- 適用的強制RULE-ID：`RULE-0001`、`RULE-0002`、`RULE-0003`、`RULE-0004`、`RULE-0006`、`RULE-0008`、`RULE-0009`、`RULE-0010`、`RULE-0011`、`RULE-0012`

## 工作中檢查點

`無`

## 外部子系統摘要

- 名稱／位置／版本：團隊 Git 專案；`C:\git\gammPlatform`；main
- 階段／阻塞：聊天室、Poker 與初步 Game System 已合併；無阻塞。
- 下一步：Pull 最新 origin/main 後只續作 `modules/game/system`。
- 詳細入口：GitHub `GamePlatform_Project` 的 main。
- 最後同步：`2026-08-28 16:13:25 +08:00`
